# AITrend — AI 트렌드 일간 리포트

**Claude 자동화가 매일 최신 AI 동향을 수집·검증·정리해 커밋하는 저장소입니다.**

이 저장소의 리포트는 **사람이 쓰지 않았습니다.** 수집부터 판정, 마크다운 작성, 커밋까지 전부 자동화가 수행합니다.

> 2026-04-17 ~ 진행 중 · 리포트 **193건**

---

## 저장 구조

```
YYYY/
└── MM/
    └── YYYY-MM-DD-ai-trend.md
```

날짜 하나에 리포트 하나. 수집 기준일은 리포트 상단에 명시됩니다.

```
2026/04/2026-04-17-AI-Trend.md   ← 최초
...
2026/09/2026-09-23-ai-trend.md   ← 최신
```

---

## 수집 소스

한 곳만 보면 편향됩니다. 성격이 다른 소스를 병렬로 훑습니다.

| 소스 | 무엇을 얻나 |
|---|---|
| Threads (AI 인플루언서 계정) | 업계 담론 · 인터뷰 · 현장 반응 |
| Hugging Face | 신규 모델 · 양자화 배포 · 저장소 커밋 이력 |
| GeekNews | 국내 개발자 커뮤니티의 관심 순위 |
| 기술 블로그 | 실무 적용 사례 |
| GitHub Trending | 실제로 쓰이기 시작한 도구 |

API가 없거나 로그인 장벽이 있는 소스가 섞여 있어, **헤드리스 브라우저로 비로그인 접근**하는 경로를 둡니다.

---

## 파이프라인 설계

### 1. 날짜 경계를 정확히 자른다

"오늘의 AI 트렌드"를 만들려면 **어제 00:00~23:59(KST)** 에 올라온 것만 골라야 합니다. 그런데 소스마다 시각 표기가 다릅니다.

| 소스 | 시각 표기 |
|---|---|
| Threads | `<time datetime="...">` — **UTC** |
| GeekNews | `<time title="...">` — **KST 절대시각** |

각 소스의 시각 속성을 파싱해 **전부 KST로 환산한 뒤 구간 필터링**합니다.

화면에 보이는 "3시간 전" 같은 상대 시각은 쓰지 않습니다. 수집 시점에 따라 날짜가 어긋나기 때문입니다.

### 2. 수집 실패를 숨기지 않는다

**이 파이프라인에서 가장 중요한 설계입니다.**

비로그인 상태에서는 최신 게시물이 4~7건만 노출되고, 스크롤하면 즉시 로그인 유도 화면으로 막힙니다. 게시 빈도가 높은 계정이면 **하루치를 다 훑지 못합니다.**

이때 리포트에 이렇게 남깁니다.

> **비고(중요)**: 이번 회차도 07-25 구간을 전혀 확인하지 못했습니다. 비로그인 상태로 노출되는 최신 게시물 7개를 전부 KST로 환산한 결과, 전부 07-26 새벽에 게시된 것으로 확인되어 목표 구간보다 뒤쪽입니다. 스크롤 2회 추가 시도했으나 즉시 로그인 유도 화면으로 막혔습니다.
>
> **결론**: 이번 회차도 이 계정에서 해당일의 실질적 이슈를 확인하지 못했습니다. 대신 GeekNews에서 확인된 이슈로 갈음합니다.

리포트에 반드시 남기는 네 가지 —

1. 무엇을 **못 봤는지**
2. **왜** 못 봤는지 (로그인 장벽, 노출 물량 한계)
3. 무엇을 **시도**했는지 (스크롤 2회 등)
4. 어떻게 **갈음**했는지 (대체 소스)

**이유** — 자동 생성 리포트가 조용히 빈 구간을 만들면, 읽는 사람은 "그날은 소식이 없었나 보다"로 오해합니다. 커버리지 실패는 누락이 아니라 **데이터의 일부**이므로 본문에 남깁니다.

### 3. 범용 뉴스가 아니라 목적에 맞춰 거른다

"오늘의 AI 뉴스" 요약은 이미 널려 있습니다. 이 리포트는 **구체적인 제약과 용도**를 기준으로 필터링합니다.

- **하드웨어 제약** — RTX 3070 / 4070에서 실제로 구동 가능한 경량 모델
- **용도** — 산업 안전 체크 VLM (화재·연기 감지, PPE 착용 여부, 낙상 감지)

그래서 모델을 나열하는 데서 끝내지 않습니다.

- 파라미터 수와 양자화 형식으로 **해당 GPU에서 돌아가는지** 판정
- 모델 저장소의 **최종 커밋일을 확인**해 "업데이트된 것처럼 보이지만 실은 신규 아님"을 걸러냄
- **역할 분담 제안** — VLM은 맥락 설명, YOLO는 실시간 박스 탐지
- 용도별 추천을 최종 가이드로 정리

### 4. 검증 경로를 항상 남긴다

각 항목에 **출처 링크 · 게시 시각(KST) · 커뮤니티 반응 지표**를 함께 기록합니다. LLM 생성물을 검증 없이 신뢰하지 않기 위한 장치입니다. 읽는 사람이 원문으로 바로 갈 수 있어야 합니다.

---

## 리포트 구성

```markdown
# AI 트렌드 일간 리포트 YYYY-MM-DD (요일)
> 수집 기준일 | 출처 목록

## 1. 주요 AI 이슈
   확인 기준 명시 (시각 환산 방식)
   — 수집 실패 시 비고로 명시

## 2. HuggingFace 신규/업데이트 모델
   경량 VLM 중심, GPU 구동 가능성 판정
   기존 모델은 최종 커밋일 대조로 신규 여부 확인
   용도별 추천 가이드

## 3. GeekNews AI 주요 이슈
   | 제목 | 포인트 | 게시(KST) | 요약 |

## 4. 기술 블로그 / GitHub Trending
```

---

## 관련 저장소

| 저장소 | 설명 |
|---|---|
| [-stock-predict-byAI](https://github.com/sgustjd2/-stock-predict-byAI) | 같은 자동화 방식을 종목 리포트에 적용 (요일별 폴더 구조) |
| [claude-skills](https://github.com/sgustjd2/claude-skills) | 재사용 Claude 스킬 · 커맨드 · 에이전트 하네스 템플릿 |
| [dev-portfolio](https://github.com/sgustjd2/dev-portfolio) | 전체 프로젝트 포트폴리오 |

---

## 알려진 한계

- **파이프라인 코드가 이 저장소에 없습니다.** 현재는 산출물(마크다운)만 보관합니다.
- Threads 비로그인 수집의 커버리지가 불안정합니다 — 게시 빈도가 높은 날은 하루치를 놓칩니다. 리포트에 매번 명시하고 있으나, 근본 해결은 안 된 상태입니다.
- 파일명 표기가 초기(`AI-Trend.md`)와 현재(`ai-trend.md`)가 다릅니다.
